package eval.cinepax.cinepax.repository.tmpClass;

import org.springframework.data.jpa.repository.JpaRepository;

import eval.cinepax.cinepax.model.tmpClass.TmpClass;

public interface TmpClassRepository extends JpaRepository<TmpClass, Integer> {
    
}
